create or replace package pkg_perf_refreshindex is

    type CS is REF CURSOR;

    procedure refreshindex(pyear varchar2,ptablecode varchar2,pindexcodenum integer);

    procedure refreshbasindex(pindexcodenum integer);

    procedure refreshbasindexcode(ptablecode varchar2,pindexcodenum integer);

end pkg_perf_refreshindex;
